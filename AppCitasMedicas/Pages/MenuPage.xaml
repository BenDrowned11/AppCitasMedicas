<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppCitasMedicas.MenuPage">

    <VerticalStackLayout
        Padding="30,0"
        Spacing="25">

                    
        <Label
            Text="App De Citas"
            FontSize="30"
            HorizontalOptions="Center"
            FontAttributes="Bold"
            Margin="0,20,0,20" />

        <Button
            Text="Reservar cita"
            Clicked="OnReserveAppointmentClicked"
            HorizontalOptions="Fill" />

        
        <Button
            Text="Consultar cita"
            Clicked="OnConsultAppointmentClicked"
            HorizontalOptions="Fill" />

        
        <Button
            Text="Cerrar sesión"
            Clicked="OnLogoutClicked"
            HorizontalOptions="Fill" />

    </VerticalStackLayout>
</ContentPage>
